{% extends "Base.jinja2" %}
{% block content %}
    <form action = {{ url_for("signin") }} method="post">
        <h2>{{ form.code }}</h2>
        <h2>{{ form.submit }}</h2>
    </form>
    <a href="{{ url_for("home") }}">Home</a>
{% endblock %}
